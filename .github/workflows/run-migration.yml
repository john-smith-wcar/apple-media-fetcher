name: run-migration
on:
  workflow_dispatch:
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: psql "${{ secrets.SUPABASE_URL }}" -f migrations/001_initial.sql
